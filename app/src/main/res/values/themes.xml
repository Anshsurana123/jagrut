<!-- Required Notice: Copyright 2026 Ansh. (https://github.com/Anshsurana123/jago) -->
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme — Neural HUD Sentient AI -->
    <style name="Theme.JagoAssistant" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color: Electric Violet -->
        <item name="colorPrimary">@color/electric_violet</item>
        <item name="colorPrimaryVariant">@color/violet_dim</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color: Neural Teal -->
        <item name="colorSecondary">@color/neural_teal</item>
        <item name="colorSecondaryVariant">@color/brand_secondary</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Abyssal Black background -->
        <item name="android:windowBackground">@color/bg_void</item>
        <item name="android:statusBarColor">@color/bg_void</item>
        <item name="android:navigationBarColor">@color/bg_void</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
        <item name="android:windowNoTitle">true</item>
        <!-- BottomNavigationView coloring -->
        <item name="bottomNavigationStyle">@style/Widget.HUD.BottomNavigation</item>
        <!-- Premium global transition animations -->
        <item name="android:windowAnimationStyle">@style/CustomActivityAnimation</item>
    </style>

    <!-- HUD BottomNavigation style -->
    <style name="Widget.HUD.BottomNavigation" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="android:background">@color/surface_dark</item>
        <item name="itemIconTint">@color/neural_teal</item>
        <item name="itemTextColor">@color/text_secondary</item>
    </style>

    <!-- Custom Activity Entrance and Exit Animations -->
    <style name="CustomActivityAnimation" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/slide_in_up</item>
        <item name="android:activityOpenExitAnimation">@anim/fade_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/fade_in</item>
        <item name="android:activityCloseExitAnimation">@anim/slide_out_down</item>
    </style>

    <!-- Jago Assistant Overlay Theme -->
    <style name="JagoOverlayTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowIsFloating">true</item>
    </style>

    <!-- Transparent Background Theme for Assistant Activity -->
    <style name="Theme.Jago.Assistant" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>
</resources>
